# 录像回放功能 - 正确的实施方案

## ⚠️ 重要说明

`大华海康代码` 目录**只是参考代码**，不需要作为独立服务运行！

正确做法：在**若依后端项目中直接集成大华SDK**。

---

## 🏗️ 正确架构

```
┌─────────────────┐
│   前端 Vue3     │
│  (mpegts.js)    │
└────────┬────────┘
         │ HTTP API
         ▼
┌─────────────────────────────┐
│  若依后端 (Spring Boot)      │
│  ┌─────────────────────┐   │
│  │  直接集成大华SDK     │   │
│  │  (JNA + .so/.dll)   │   │
│  └──────────┬──────────┘   │
└─────────────┼───────────────┘
              │ SDK直接调用
              ▼
      ┌──────────────────┐
      │   大华设备/NVR   │
      │   (录像存储)     │
      └──────────────────┘
              ▲
              │ RTSP/GB28181
      ┌───────┴──────┐
      │ ZLMediaKit   │
      └──────────────┘
```

---

## 📋 实施步骤

### 步骤1：准备大华SDK库文件

#### 1.1 找到大华SDK库文件

在参考代码中找到：
```
大华海康代码/大华海康代码/大华JAVA_SDK代码/MediaManagerServer/
  libs/
    ├── linux64/
    │   ├── libdhnetsdk.so
    │   ├── libPlayCtrl.so
    │   └── ...
    └── win64/
        ├── dhnetsdk.dll
        ├── PlayCtrl.dll
        └── ...
```

#### 1.2 复制到若依项目

创建目录并复制：
```bash
mkdir -p ruoyi-vue-pro/yudao-module-iot/yudao-module-iot-biz/src/main/resources/native/linux64
mkdir -p ruoyi-vue-pro/yudao-module-iot/yudao-module-iot-biz/src/main/resources/native/win64

# 复制库文件
cp 大华海康代码/.../libs/linux64/* ruoyi-vue-pro/.../resources/native/linux64/
cp 大华海康代码/.../libs/win64/* ruoyi-vue-pro/.../resources/native/win64/
```

### 步骤2：添加Maven依赖

在 `pom.xml` 中添加：

```xml
<dependencies>
    <!-- JNA - 用于调用本地库 -->
    <dependency>
        <groupId>net.java.dev.jna</groupId>
        <artifactId>jna</artifactId>
        <version>5.12.1</version>
    </dependency>
</dependencies>
```

### 步骤3：复制并简化大华SDK代码

从参考代码复制以下核心文件：

#### 3.1 NetSDKLib.java（SDK接口定义）

```java
// 位置：cn/iocoder/yudao/module/iot/framework/dahua/NetSDKLib.java
// 从参考代码中复制：大华海康代码/.../DHSDK/NetSDKLib.java
// 内容太长，直接复制整个文件即可
```

#### 3.2 ToolKits.java（工具类）

```java
// 位置：cn/iocoder/yudao/module/iot/framework/dahua/ToolKits.java
// 从参考代码中复制
package cn.iocoder.yudao.module.iot.framework.dahua;

public class ToolKits {
    public static String getErrorCodePrint() {
        // 从参考代码复制实现
    }
}
```

#### 3.3 RecordingQueryService.java（新建 - 录像查询封装）

```java
package cn.iocoder.yudao.module.iot.service.video;

import cn.iocoder.yudao.module.iot.framework.dahua.NetSDKLib;
import cn.iocoder.yudao.module.iot.framework.dahua.ToolKits;
import com.sun.jna.ptr.IntByReference;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;

import javax.annotation.PostConstruct;
import java.text.SimpleDateFormat;
import java.util.*;

@Service
@Slf4j
public class DahuaRecordingQueryService {

    private static NetSDKLib netsdk = NetSDKLib.NETSDK_INSTANCE;
    private static final SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    
    // 登录句柄缓存（简单实现，生产环境建议使用连接池）
    private Map<String, NetSDKLib.LLong> loginHandles = new HashMap<>();

    @PostConstruct
    public void init() {
        // 初始化SDK
        boolean initResult = netsdk.CLIENT_Init(null, null);
        if (!initResult) {
            log.error("[大华SDK] 初始化失败");
        } else {
            log.info("[大华SDK] 初始化成功");
        }
        
        // 设置连接超时和重连
        netsdk.CLIENT_SetConnectTime(3000, 3);
        netsdk.CLIENT_SetAutoReconnect(null, null);
    }

    /**
     * 查询录像文件列表
     */
    public List<Map<String, Object>> queryRecordFiles(
            String deviceIp, 
            int devicePort, 
            String username, 
            String password,
            int channelNo, 
            String startTime, 
            String endTime) {
        
        List<Map<String, Object>> result = new ArrayList<>();
        NetSDKLib.LLong loginHandle = null;
        
        try {
            // 1. 登录设备
            loginHandle = login(deviceIp, devicePort, username, password);
            if (loginHandle.longValue() == 0) {
                log.error("[大华SDK] 设备登录失败 {}:{}", deviceIp, devicePort);
                return result;
            }
            
            // 2. 转换时间格式
            NetSDKLib.NET_TIME stTimeStart = convertToNetTime(startTime);
            NetSDKLib.NET_TIME stTimeEnd = convertToNetTime(endTime);
            
            // 3. 查询录像文件
            int maxRecordCount = 2000;
            NetSDKLib.NET_RECORDFILE_INFO[] fileInfoArray = 
                (NetSDKLib.NET_RECORDFILE_INFO[]) new NetSDKLib.NET_RECORDFILE_INFO().toArray(maxRecordCount);
            IntByReference findCount = new IntByReference(0);
            
            boolean success = netsdk.CLIENT_QueryRecordFile(
                loginHandle,
                channelNo,
                0,  // 录像类型：0-所有录像
                stTimeStart,
                stTimeEnd,
                null,
                fileInfoArray,
                maxRecordCount * fileInfoArray[0].size(),
                findCount,
                5000,  // 超时5秒
                false
            );
            
            if (success) {
                int count = findCount.getValue();
                log.info("[大华SDK] 查询成功，找到 {} 个录像文件", count);
                
                // 4. 转换结果
                for (int i = 0; i < count; i++) {
                    NetSDKLib.NET_RECORDFILE_INFO fileInfo = fileInfoArray[i];
                    
                    Map<String, Object> record = new HashMap<>();
                    record.put("channelId", fileInfo.ch);
                    record.put("fileName", new String(fileInfo.filename).trim());
                    record.put("fileSize", fileInfo.size);
                    record.put("startTime", formatNetTime(fileInfo.starttime));
                    record.put("endTime", formatNetTime(fileInfo.endtime));
                    record.put("recordType", fileInfo.driveno);
                    record.put("diskNo", fileInfo.diskno);
                    
                    result.add(record);
                }
            } else {
                log.error("[大华SDK] 查询录像失败: {}", ToolKits.getErrorCodePrint());
            }
            
        } catch (Exception e) {
            log.error("[大华SDK] 查询异常", e);
        } finally {
            // 注意：这里不立即注销，保持连接复用
            // 生产环境建议实现连接池管理
        }
        
        return result;
    }

    /**
     * 登录设备
     */
    private NetSDKLib.LLong login(String ip, int port, String username, String password) {
        String key = ip + ":" + port;
        
        // 检查是否已登录
        if (loginHandles.containsKey(key)) {
            NetSDKLib.LLong handle = loginHandles.get(key);
            if (handle.longValue() != 0) {
                return handle;
            }
        }
        
        // 执行登录
        NetSDKLib.NET_DEVICEINFO_Ex deviceInfo = new NetSDKLib.NET_DEVICEINFO_Ex();
        NetSDKLib.LLong loginHandle = netsdk.CLIENT_LoginEx2(
            ip, port, username, password, 
            0, null, deviceInfo, null
        );
        
        if (loginHandle.longValue() != 0) {
            log.info("[大华SDK] 设备登录成功 {}:{}", ip, port);
            loginHandles.put(key, loginHandle);
        } else {
            log.error("[大华SDK] 设备登录失败 {}:{}, 错误: {}", 
                ip, port, ToolKits.getErrorCodePrint());
        }
        
        return loginHandle;
    }

    /**
     * 字符串时间转 NET_TIME
     */
    private NetSDKLib.NET_TIME convertToNetTime(String timeStr) {
        NetSDKLib.NET_TIME netTime = new NetSDKLib.NET_TIME();
        try {
            Date date = sdf.parse(timeStr);
            Calendar cal = Calendar.getInstance();
            cal.setTime(date);
            
            netTime.dwYear = cal.get(Calendar.YEAR);
            netTime.dwMonth = cal.get(Calendar.MONTH) + 1;
            netTime.dwDay = cal.get(Calendar.DAY_OF_MONTH);
            netTime.dwHour = cal.get(Calendar.HOUR_OF_DAY);
            netTime.dwMinute = cal.get(Calendar.MINUTE);
            netTime.dwSecond = cal.get(Calendar.SECOND);
        } catch (Exception e) {
            log.error("[时间转换] 失败: {}", timeStr, e);
        }
        return netTime;
    }

    /**
     * NET_TIME 转字符串
     */
    private String formatNetTime(NetSDKLib.NET_TIME netTime) {
        return String.format("%04d-%02d-%02d %02d:%02d:%02d",
            netTime.dwYear, netTime.dwMonth, netTime.dwDay,
            netTime.dwHour, netTime.dwMinute, netTime.dwSecond);
    }
}
```

### 步骤4：创建Controller

```java
package cn.iocoder.yudao.module.iot.controller.admin.video;

import cn.iocoder.yudao.framework.common.pojo.CommonResult;
import cn.iocoder.yudao.module.iot.service.video.DahuaRecordingQueryService;
import cn.iocoder.yudao.module.iot.dal.dataobject.channel.ChannelDO;
import cn.iocoder.yudao.module.iot.dal.dataobject.device.DeviceDO;
import cn.iocoder.yudao.module.iot.service.channel.ChannelService;
import cn.iocoder.yudao.module.iot.service.device.DeviceService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.extern.slf4j.Slf4j;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import javax.annotation.Resource;
import java.util.*;

import static cn.iocoder.yudao.framework.common.pojo.CommonResult.success;

@Tag(name = "管理后台 - 摄像头录像（大华SDK）")
@RestController
@RequestMapping("/iot/camera/recording")
@Validated
@Slf4j
public class CameraRecordingDahuaController {

    @Resource
    private DahuaRecordingQueryService dahuaRecordingQueryService;
    
    @Resource
    private ChannelService channelService;
    
    @Resource
    private DeviceService deviceService;

    @PostMapping("/query-dahua-files")
    @Operation(summary = "查询录像文件列表（大华SDK）")
    public CommonResult<List<Map<String, Object>>> queryDahuaRecordingFiles(
            @RequestBody Map<String, Object> params
    ) {
        try {
            // 1. 获取参数
            Integer channelId = (Integer) params.get("channelId");
            String startTime = (String) params.get("startTime");
            String endTime = (String) params.get("endTime");
            
            if (channelId == null || startTime == null || endTime == null) {
                return CommonResult.error(400, "参数不完整：需要 channelId, startTime, endTime");
            }
            
            // 2. 查询通道信息
            ChannelDO channel = channelService.getChannel(channelId.longValue());
            if (channel == null) {
                return CommonResult.error(404, "通道不存在");
            }
            
            // 3. 查询设备信息
            DeviceDO device = deviceService.getDevice(channel.getDeviceId());
            if (device == null) {
                return CommonResult.error(404, "设备不存在");
            }
            
            // 4. 调用大华SDK查询录像
            List<Map<String, Object>> recordFiles = dahuaRecordingQueryService.queryRecordFiles(
                device.getIp(),
                device.getPort(),
                device.getUsername(),
                device.getPassword(),
                channel.getChannelNo(),
                startTime,
                endTime
            );
            
            log.info("[录像查询] 通道ID:{}, 设备:{}, 查询到 {} 条录像", 
                channelId, device.getIp(), recordFiles.size());
            
            return success(recordFiles);
            
        } catch (Exception e) {
            log.error("[录像查询] 异常", e);
            return CommonResult.error(500, "查询录像异常: " + e.getMessage());
        }
    }
}
```

### 步骤5：配置本地库路径

在 `application.yml` 中添加：

```yaml
# 大华SDK本地库路径配置
dahua:
  sdk:
    native-lib-path: native/${os.name}/${os.arch}
```

---

## 🚀 启动流程（正确版本）

```bash
# ❌ 不需要：启动大华SDK服务（参考代码不启动）

# ✅ 只需要启动这两个：

# 1️⃣ 启动若依后端（已集成大华SDK）
cd ruoyi-vue-pro
mvn spring-boot:run

# 2️⃣ 启动前端
cd yudao-ui-admin-vue3
npm run dev
```

---

## ✅ 验证步骤

### 1. 检查SDK初始化
查看若依后端启动日志：
```
[大华SDK] 初始化成功
```

### 2. 测试录像查询接口
```bash
curl -X POST http://localhost:48080/admin-api/iot/camera/recording/query-dahua-files \
  -H "Content-Type: application/json" \
  -d '{
    "channelId": 1,
    "startTime": "2026-01-22 00:00:00",
    "endTime": "2026-01-22 23:59:59"
  }'
```

### 3. 前端测试
- 打开录像回放页面
- 拖拽通道到窗口
- 观察控制台日志：
  ```
  [录像检查-大华SDK] 通道 1 - 有录像，共 10 个文件
  ```

---

## 📁 最终项目结构

```
ruoyi-vue-pro/yudao-module-iot/yudao-module-iot-biz/
├── src/main/java/cn/iocoder/yudao/module/iot/
│   ├── controller/admin/video/
│   │   └── CameraRecordingDahuaController.java    (新增)
│   ├── service/video/
│   │   └── DahuaRecordingQueryService.java        (新增)
│   └── framework/dahua/
│       ├── NetSDKLib.java                         (从参考代码复制)
│       └── ToolKits.java                          (从参考代码复制)
└── src/main/resources/
    └── native/
        ├── linux64/
        │   ├── libdhnetsdk.so
        │   └── ...
        └── win64/
            ├── dhnetsdk.dll
            └── ...
```

---

## 🎯 核心要点

1. **不启动参考代码项目** - 只用来参考和复制文件
2. **直接在若依后端集成** - 通过JNA调用本地库
3. **复制必要的文件**：
   - NetSDKLib.java（SDK接口定义）
   - 本地库文件（.so/.dll）
   - 封装Service层
4. **保持简单** - 不需要额外的微服务

---

## 💡 优势

- ✅ 架构简单：少一个服务
- ✅ 维护方便：代码集中在若依项目
- ✅ 性能更好：减少HTTP调用开销
- ✅ 部署简单：只需部署若依后端

---

## ⚠️ 注意事项

1. **JNA库路径**：确保应用能找到 .so/.dll 文件
2. **设备信息**：从数据库读取设备IP、端口、账号密码
3. **连接管理**：建议实现设备连接池（生产环境）
4. **异常处理**：SDK调用可能失败，需要完善异常处理

---

## 📞 下一步

1. 复制 NetSDKLib.java 和库文件到若依项目
2. 创建 DahuaRecordingQueryService
3. 创建 CameraRecordingDahuaController
4. 测试接口
5. 前端验证

详细代码都已经提供，按步骤复制即可！
