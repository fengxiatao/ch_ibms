# 项目规则

## MCP 数据库操作规则

- 当前项目的数据库操作统一使用 `ch_mysql` MCP Server 直接执行。
- 在 Trae 当前工具环境中，对应的服务标识为 `mcp_ch_mysql`。
- 涉及查询、补数、校验、更新等数据库动作时，优先通过 MCP 执行，不使用本地命令行直连 MySQL。
- 除非用户明确要求，否则不要改用其他数据库连接方式或额外的数据库操作工具。
